setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    POSTGRES_PASSWORD: postgres
build:
  docker:
    web: Dockerfile
    graphql-engine: hasura/graphql-engine:latest
run:
  web: bundle exec puma -C config/puma.rb
